@model IEnumerable<BlogApp.ViewModels.PostListItem>

@{
    ViewBag.Title = "Blog App";
    ViewBag.Heading = "Blog Home";
    ViewBag.SubHeading = "Built with ASP.NET MVC 5";
}

<!-- Recent Posts -->
@if (Model.Count() > 0)
{
    <h1>Recent Updates</h1>
    <hr />
    @Html.Partial("../Posts/_PostListPartial", Model)
}

